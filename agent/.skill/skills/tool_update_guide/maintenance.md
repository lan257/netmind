# 维护说明

- 不要只改 `run.py`；同一 Tool 文件夹内的定义和文档也要同步。
- 不要只改 `tool_definition.json` 声明新功能；脚本必须真实实现。
- 参数重命名时检查权限文案、示例、测试和历史调用兼容性。
- 权限级别降低时要重新评估风险，尤其是文件写入、删除、网络和系统命令。
- 列表和绑定不在基本流程中手改。
- 长文本写入使用 `long_text_line_modifier_guide` 拆块，避免单次编辑超出限制。
- 如果 Tool 不再执行任何动作，应迁移为 prompt-only Skill，而不是保留空的 `run.py`。
